
 Edit this configuration file replacing <username value> with synapse
 username and <password value> with password. To prevent credential
 leakage, run 

 $git update-index --assume-unchanged config/SynapseCredentials.dat

 from the repo root.

